# ============================================================
#  Kho Prompt N-Edit — Biến môi trường
#  Copy file này thành .env rồi điền giá trị thật.
#  (KHÔNG commit file .env lên git — chỉ commit .env.example)
# ============================================================

# --- Supabase (nơi lưu prompt của cả team) ---------------------------------
# Lấy ở: Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# --- Backend AI (endpoint để "Nhờ AI viết giúp") ---------------------------
# IT sẽ viết endpoint này. Nó gọi Anthropic API bằng key giữ ở server
# (KHÔNG bao giờ để ANTHROPIC_API_KEY ở frontend).
VITE_AI_API_URL=/api/ai/suggest-prompt

# ============================================================
#  Các biến CHỈ dùng ở backend (server IT) — KHÔNG có tiền tố VITE_,
#  KHÔNG để lộ ra frontend. Ghi ở đây để IT biết cần chuẩn bị gì.
# ============================================================
# ANTHROPIC_API_KEY=sk-ant-xxxxx
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # nếu backend cần ghi/đọc bảo mật
